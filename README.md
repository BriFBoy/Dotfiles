
# BriFBoy's Dotfiles

Personal dotfiles for an Arch Linux + Hyprland setup. Not really meant to be used
by someone else, but if you want to try them out there is an install script
(`install.sh`).

Colors are generated by [pywal](https://dylanaraps.github.io/pywal/) from the
current wallpaper and applied to Hyprland with
[Noctalia](https://noctalia.kalmari.ghost/). A built in wallpaper switcher
(Super+W) re-themes everything.

## Stack

- **Desktop:** Hyprland (Lua config) + Noctalia + pywal, `ly` login and
  `hyprlock`/`hypridle`/`swww`/`wlogout`
- **Terminal:** Ghostty
- **Editor:** tmux + Neovim (work in progress)
- **Shell:** Bash (a Fish config is included too)
- **File manager:** yazi (run `y` in bash), dolphin
- **Bar / notifications:** Waybar, Dunst
- **Misc:** Rofi, Cava, Fastfetch, Starship, udiskie, Floorp

## Screenshots

The normal desktop
![Screenshots of the desktop](https://github.com/BriFBoy/Dotfiles/blob/readme/doc/desktop.png?raw=true)

Screenshots of inside neovim
![Screenshots of inside neovim](https://github.com/BriFBoy/Dotfiles/blob/readme/doc/in-neovim.png?raw=true)

## Installation
To install the dotfiles use the install script or install them manually with
stow (`stow <package>`, e.g. `stow hyprland ghostty bash`).

### Script info
If you want to try out my dotfiles there is a few things that are needed. The
script ONLY works on an Arch based system.
- An Arch based system. A clean Arch install is recommended
- Or a setup with no dotfiles pre installed
The script will delete your existing dotfiles, so make a backup first.
You will need to manually remove your old display manager if you try to use the
install script on an existing setup.

The script will ask you if you want stuff that is not necessary, but useful in
certain situations. When it asks you can either type "y" or press enter to
accept. If you want to deny, just type anything else.

### Step by step
Clone the repo using git. The install script only works if the dotfiles are
located in the .dotfiles dir
```bash
  git clone --depth 1 https://github.com/BriFBoy/Dotfiles .dotfiles
  cd .dotfiles
```

Make the script runnable and run the script. You will need to enter your
password to install some of the necessary packages
```bash
  chmod u+x ./install.sh
  ./install.sh
  # or just run the script directly with
  bash install.sh
```
When the script is done. Reboot to apply the changes.
```bash
  reboot
```
### Note
When logging into hyprland for the first time you will need to set the
wallpaper. You can do this by pressing Super+W and selecting a wallpaper.
